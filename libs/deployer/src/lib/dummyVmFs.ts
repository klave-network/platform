export const dummyMap: Record<string, string | null> = {
    'asconfig.json': `
        {
            "options": {
                "bindings": "esm",
                "disable": "bulk-memory"
            }
        }`
};